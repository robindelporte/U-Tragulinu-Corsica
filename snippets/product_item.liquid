<script src="{{ 'li_custom.js' | asset_url }}" type="text/javascript"></script>
<div li-element="product-variant-container" class="product-card_item" x-data="handleVariant({{ product | json | escape }}, {{ product.options_by_name | json | escape }}, {{ product.selected_or_first_available_variant | json | escape }})" x-init="$nextTick(() => { activateVariants() })" @setbundleitem.window="updateBundleItem($event.detail.product, $event.detail.quantity)">
                          <div class="product-item_image-link">
                            <a li-object:href="product.url" href="{{ product.url }}" class="product-item_image-wrapper w-inline-block"><img li-object:src="product.featured_image | img_url: 'master'" li-object:alt="product.featured_image.alt" alt="{{ product.featured_image.alt }}" src="{{ product.featured_image | img_url: 'master' }}" loading="lazy" class="product-item_image"></a>
                            <div class="product-card-item_tag_list">
                              {% if product.metafields.custom.precommande_disponible == true %}    <div li-if="product.metafields.custom.precommande_disponible == true">
    
                                <div class="tag">Précommande</div>
                                  </div>
{% endif %}
                              {% if product.metafields.custom.saison == false %}    <div li-if="product.metafields.custom.saison == false">
    
                                <div class="tag">Hors saison</div>
                                  </div>
{% endif %}
                              {% if product.selected_or_first_available_variant.available == false %}    <div li-if="product.selected_or_first_available_variant.available == false">
    
                                <div class="tag">En rupture de stock</div>
                                  </div>
{% endif %}
                            </div>
                          </div>
                          <div class="product-item_bottom">
                            <div class="loox_parent hide-designer"></div>
                            <a li-object:href="product.url" href="{{ product.url }}" class="product-item_text-link w-inline-block">
                              <div class="product_item-name">
                                <div li-object="product.title" class="text-style-tagline">{{ product.title }}</div>
                                {% if product.metafields.custom.conditionnement_cartes %}    <div li-if="product.metafields.custom.conditionnement_cartes">
    
                                  <div li-object="product.metafields.custom.conditionnement_cartes" class="text-size-tiny text-style-italic">{{ product.metafields.custom.conditionnement_cartes }}</div>
                                    </div>
{% endif %}
                              </div>
                              <div class="product-item_price">
                                <div li-object="product.price | money" class="text-style-tagline">{{ product.price | money }}</div>
                              </div>
                            </a>
                          </div>
                        </div>
